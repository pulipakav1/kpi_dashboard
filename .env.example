# PostgreSQL connection
DATABASE_URL=postgresql://postgres:password@localhost:5432/saas_analytics

# dbt profile credentials (mirrors profiles.yml.example)
DBT_USER=postgres
DBT_PASSWORD=password
DBT_HOST=localhost
DBT_PORT=5432
DBT_DBNAME=saas_analytics
